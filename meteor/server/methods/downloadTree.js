import {
    Model
} from '../../lib/collections/model'

Meteor.methods({
	/**
	 * queries the database for the derivation tree of the current model
	 * @param {String} modelId the model to act as root of the tree
	 */
    downloadTree: function(modelId) {
		
    }
});